doker build -t nomImatge $path	//PEr a crear una imatge
	Exemple: doker build -t tsr2021/ubuntu-zmq .	//El punt es el path

docker images			//per a veure les images creades amb anterioritat

docker run nomImatge		//Llança la execucio de un contenidor

docker ps -a			//VEure el estat dels contenidors. Podem treure la idCont

docker inspect idCont	//Detalls del contenidor, per veure la IP per exemple

docker stop idCont	//Detindre contenidor
	docker kill $(docker ps -q)	//Pararlos tots de tiró

docker rm idCont	//Borrar contenidor
	docker rm $(docker ps -a -q)	//Borrarlos tots

docker rmi idImatge	//Eliminar una imatge

docker-compose up --scale cli=4 --scale wor=2

sudo shutdown -H

docker exec -it 2_cbw_bro_1 /bin/bash esto es para entrar al contenedor y hacer cosas por comando

Ctrl+R	Busqueda reversa de comandos en la consola

Si en el DockerFile reutilitze imatges, borrar la imatge anterior per a que la refaça. Pot fallar si intentes borrar 
una imatge i te contenidors actius. Els contenidors actius se tenen que parar abans de poder ser borrats.